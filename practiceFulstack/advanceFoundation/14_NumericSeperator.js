/**
 * numeric separator
 * 
 * numeric separator is a feature in JavaScript that allows you to use underscores (_) as separators in numeric literals to improve readability. This can be especially helpful when dealing with large numbers, making them easier to read and understand at a glance.
 *  For example, instead of writing 1000000, you can write 1_000_000, which is more visually appealing and easier to comprehend. The numeric separator does not affect the actual value of the number; it is purely for readability purposes.
 *  You can use numeric separators in various types of numeric literals, including integers, floating-point numbers, and even binary, octal, and hexadecimal literals. For example:
 * const million = 1_000_000;
 * const pi = 3.14_15_92;
 * const binary = 0b1010_1010;
 * const octal = 0o12_345;
 * const hex = 0xDE_AD_BE_EF;
 * In this example, the numeric separators make it easier to read the values of the variables, especially for large numbers or those with many digits. The use of numeric separators can enhance code readability and maintainability, making it easier for developers to understand the intent of the code at a glance.
 * 
 * In summary, numeric separators are a useful feature in JavaScript that allows you to improve the readability of numeric literals by using underscores as visual separators. This can help make your code more understandable and easier to maintain, especially when dealing with large numbers or complex numeric values. 
 * 
 */

