/**
 * pre-increment operator
 * The pre-increment operator (++) is a unary operator that increments the value of a variable by 1 before it is used in an expression. The syntax for the pre-increment operator is as follows:
 * ++variable;
 * For example:
 * let x = 5;
 * console.log(++x); // 6
 * In this example, the value of x is incremented by 1 before it is logged to the console, resulting in an output of 6. If we were to use the post-increment operator (x++), the value of x would be logged first and then incremented, resulting in an output of 5 followed by x being updated to 6.
 * The pre-increment operator is often used in loops and other situations where you want to increment a variable before using it in an expression. It can help improve readability and reduce the chances of off-by-one errors in certain cases.
 * In summary, the pre-increment operator (++) increments the value of a variable by 1 before it is used in an expression, making it a useful tool for various programming scenarios where you need to update a variable before using it. 
 * Example:
 * let count = 0;
 * console.log(++count); // 1
 * console.log(count); // 1
 * In this example, the pre-increment operator increments the value of count from 0 to 1 before it is logged to the console. The second log statement confirms that count has been updated to 1.
 * 
 * 
 */